const config = {
  plugins: ["@tailwindcss/postcss"],
};
// This file configures PostCSS to use Tailwind CSS.
// Tailwind CSS is a "utility-first" CSS framework that makes it easy to style
// components and pages.
//
// The plugins array is used to specify which PostCSS plugins to use.
// The "@tailwindcss/postcss" plugin is used to tell Tailwind CSS to generate
// CSS based on the configuration in the tailwind.config.mjs file.

export default config;
